import { TitleCastPipe } from './title-cast.pipe';

describe('TitleCastPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCastPipe();
    expect(pipe).toBeTruthy();
  });
});
